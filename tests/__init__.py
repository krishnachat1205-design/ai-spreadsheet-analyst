"""
Tests Package
=============

Unit and integration tests for the AI Spreadsheet Analyst application.

Future responsibilities:
------------------------
- Test each service module in isolation with fixture DataFrames.
- Integration tests for the upload → clean → analyze → export pipeline.
- Configuration and path resolution tests for core/config.py.
- Use pytest as the test runner (add to requirements-dev.txt when ready).
"""
