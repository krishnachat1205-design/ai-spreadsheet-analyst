"""
App Package — Streamlit UI Layer
=================================

Contains Streamlit-specific UI components, pages, and layout helpers.
This package depends on `services/` but must not contain business logic.

Future responsibilities:
------------------------
- Page modules for upload, dashboard, insights, and export workflows.
- Reusable Streamlit widgets (file uploader wrapper, metric cards, charts).
- Sidebar navigation and session-state helpers scoped to the UI.
- Styling hooks that reference assets/ (CSS, images, icons).
"""
